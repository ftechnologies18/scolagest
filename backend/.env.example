# Variables d'environnement — Backend Go
# Copier ce fichier en .env et adapter les valeurs (surtout JWT_SECRET en production).

# Port d'écoute du backend Go
PORT=8080

# Chemin de la base SQLite (en développement local)
# En production : remplacer par une URL PostgreSQL (Neon, Supabase, etc.)
DB_PATH=./data/scolagest.db

# Secret JWT — OBLIGATOIRE à changer en production (chaîne aléatoire longue)
JWT_SECRET=change-me-to-a-long-random-secret-in-production

# Durée de vie des tokens (en heures)
JWT_ACCESS_EXP_HR=1
JWT_REFRESH_EXP_HR=168

# Environnement : development | production
APP_ENV=development
