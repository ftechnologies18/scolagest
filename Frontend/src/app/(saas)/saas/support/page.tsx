"use client";

/**
 * ScolaGest — Mode Support SaaS (route `/saas/support`).
 */

import SaasSupportView from "@/components/dashboard/views/view-saas-support";

export default function SaasSupportPage() {
  return <SaasSupportView />;
}
