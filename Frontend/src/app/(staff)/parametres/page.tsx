"use client";

/**
 * ScolaGest — Page paramètres (route `/parametres`).
 */

import ParametresView from "@/components/dashboard/views/view-parametres";

export default function ParametresPage() {
  return <ParametresView />;
}
