"use client";

/**
 * ScolaGest — Page caisse (route `/caisse`).
 *
 * Rend la vue `CaisseView` existante.
 */

import CaisseView from "@/components/dashboard/views/view-caisse";

export default function CaissePage() {
  return <CaisseView />;
}
