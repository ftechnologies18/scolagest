"use client";

/**
 * ScolaGest — Page années scolaires (route `/annees`).
 */

import AnneesView from "@/components/dashboard/views/view-annees";

export default function AnneesPage() {
  return <AnneesView />;
}
